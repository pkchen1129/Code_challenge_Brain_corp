#include<iostream> 
#include<math.h>
#include<vector>
#include <chrono> 
using namespace std;

vector< vector<int> > naive_method(vector<vector<int> > v1, vector<vector<int> > v2);
vector< vector<int> > transpose(vector<vector<int> > v1);
vector< vector<int> > loop_permutation(vector<vector<int> > v1, vector<vector<int> > v2);